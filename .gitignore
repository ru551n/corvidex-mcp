# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
build/
dist/

# Tooling
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Benchmark tooling output (tools/*.py --out)
runs/

# Bundled model ONNX weights (provisioned into src/corvidex_mcp/assets/
# by tools/bundle_model.py on the machine that builds the wheel; the
# wheel still includes them). The small tokenizer/config JSON files in
# the same directory are committed.
src/corvidex_mcp/assets/*/onnx/
